<%= form_with(model:[park, park_comment], local: false) do |f| %>
<div class="form-group">
  <%= f.text_area :comment, rows:'5',placeholder: "コメントをここに", class: "form-cotrol w-100 comment-textarea" %>
</div>
  <%= f.submit "送信する", class: 'btn btn-info float-right' %>
<% end %>
